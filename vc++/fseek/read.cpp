#include <stdio.h>
#include <fstream>
#include <iostream>
#include <string>
using namespace std;

int main ()
{
   string f = "file.txt";

   int * s;
   fgets(s, 5, "r");
   printf(s);
   fclose(fp);

   return(0);
}
